# DISABLED: This repo uses local CI only (no GitHub Actions minutes).
# Run: npm run ci   or   pwsh ./scripts/ci.ps1
#
# To avoid accidental cloud runner usage, automatic triggers are removed.
# workflow_dispatch is retained only as a manual escape hatch if you re-enable Actions later.

name: Test (disabled — use local CI)

on:
  workflow_dispatch:

jobs:
  local-ci-reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Use local CI instead
        run: |
          echo "GitHub Actions is disabled for this repository."
          echo "From the repo root run: npm run ci"
          echo "Or: pwsh ./scripts/ci.ps1"
          exit 1
